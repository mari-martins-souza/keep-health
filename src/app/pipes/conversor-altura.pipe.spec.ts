import { ConversorAlturaPipe } from './conversor-altura.pipe';

describe('ConversorAlturaPipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorAlturaPipe();
    expect(pipe).toBeTruthy();
  });
});
