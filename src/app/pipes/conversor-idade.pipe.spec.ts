import { ConversorIdadePipe } from './conversor-idade.pipe';

describe('ConversorIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new ConversorIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
